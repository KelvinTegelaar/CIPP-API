{
  "name": "AuthMethodsPolicyMigration",
  "label": "Complete Authentication Methods Policy Migration",
  "cat": "Entra (AAD) Standards",
  "tag": [],
  "impact": "Medium Impact",
  "helpText": "Completes the migration of authentication methods policy to the new format. This is required when migrating from legacy authentication policies to the new unified authentication methods policy.",
  "executiveText": "Completes the transition from legacy authentication policies to Microsoft's modern unified authentication methods policy, ensuring the organization benefits from the latest security features and management capabilities. This migration enables enhanced security controls and simplified policy management.",
  "recommendedBy": [
    "CIPP"
  ],
  "requiredCapabilities": [],
  "secureScoreImpact": 0,
  "compare": "subset",
  "variables": {
    "migrationState": {
      "type": "autoComplete",
      "label": "Authentication methods policy migration state",
      "options": [
        {
          "label": "Migration Complete",
          "value": "migrationComplete"
        }
      ],
      "default": "migrationComplete",
      "recommended": "migrationComplete"
    }
  },
  "expected": {
    "policyMigrationState": "%migrationState%"
  },
  "read": {
    "cacheType": "AuthenticationMethodsPolicy"
  },
  "remediate": {
    "executor": "GraphRequest",
    "requests": [
      {
        "method": "PATCH",
        "uri": "policies/authenticationMethodsPolicy",
        "body": {
          "policyMigrationState": "%migrationState%"
        }
      }
    ]
  }
}
